- add unfocus action (only applies to focused session)
- update sessions to have only a single subject
- session creation
  - the list of available subjects should be allowed to fill the screen
  - sort subjects in session creation by recently created (so that a subject created during
    the create session flow is at the top)
  - selecting a subject in the create session flow should give a "create session with
    <subject>" prompt and then create the session (rather than adding the subject to a
    list and allowing further subject selection)
- open assets folders from session board
  - would be nice to import automatically and continously from photos lib for this to work
    well (to see photos from past samples while taking present sample)
- session board
  - put the key key on the same line as the SESSIONS header
    - format: "[key] description // [key] description // ..."
